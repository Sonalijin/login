import React from "react";

import ProfileLook from "./Profile-Look";

const Loggin = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <ProfileLook />
    </div>
  );
};
export default Loggin;
